# Contracts: AIB-277

No new API endpoints are introduced by this feature.

The PricingSection is a server component that imports plan data directly from `lib/billing/plans.ts`.
Existing endpoint `GET /api/billing/plans` is unchanged and not consumed by this feature.
