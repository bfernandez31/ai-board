# AI-BOARD Assist Result

## Status
SUCCESS

## Message
@Benoît Fernandez Fixed both issues: added Unauthorized error handling to 3 GET routes and added pendingLaunches pruning after terminal job detection.

## Files Modified
- app/api/projects/[projectId]/comparisons/route.ts
- app/api/projects/[projectId]/comparisons/[comparisonId]/route.ts
- app/api/projects/[projectId]/comparisons/candidates/route.ts
- components/comparison/project-comparisons-page.tsx

## Summary
1. Added 'Unauthorized' catch clause to comparisons/route.ts, comparisons/[comparisonId]/route.ts, and comparisons/candidates/route.ts — matching the pattern already used in launch/route.ts (returns 401 AUTH_REQUIRED instead of falling through to 500).
2. Added terminal launch pruning in the hasTerminalPendingJob effect — after invalidating queries, terminal launches are removed from pendingLaunches state so hasTerminalPendingJob resets to false, allowing subsequent launches to trigger list refresh correctly.
