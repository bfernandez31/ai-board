# AI-BOARD Assist Result

## Status
SUCCESS

## Message
@Benoît Fernandez Fixed warning message wording to match the `.strip()` schema behavior — "will be ignored" changed to "will be stripped" in all 3 locations.

## Files Modified
- lib/validations/config.ts
- specs/AIB-478-project-config-in/contracts/config-sync-api.md

## Summary
Updated `collectUnknownFieldWarnings` warning messages in config.ts (2 occurrences) and the API contract example in config-sync-api.md (1 occurrence) to say "will be stripped" instead of "will be ignored", matching the `.strip()` behavior of `ProjectConfigSchema`.
