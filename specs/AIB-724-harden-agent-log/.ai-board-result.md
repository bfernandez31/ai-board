# AI-BOARD Assist Result

## Status
SUCCESS

## Message
@[81817281:Benoît Fernandez] Review fixes applied: 2 fixed, 0 specs updated, 2 rejected

## Files Modified
- app/lib/logs/schema.ts
- .github/scripts/capture-agent-logs.sh

## Summary
Fixed 2 review findings from ai-board (2), Codex (0), Copilot (0).
0 spec files updated for contract consistency.
2 findings rejected: duplicates of higher-priority ai-board findings.

Fix #1: Aligned `PREVIEW_INPUT_MAX_CHARS` (4096 → 320) with `JobLog.preview @db.VarChar(320)` per constitution §IV rule that Zod constraints must match DB column constraints.

Fix #2: Restored preview length cap in `derive_preview()` — `truncate()` now slices to `PREVIEW_MAX_CHARS` (280) and appends an ellipsis, mirroring `app/lib/logs/preview.ts`. Prevents long error/message text from exceeding the server Zod max and orphaning the uploaded Blob artifact.

Rejections:
- #3 (Codex P1, capture-agent-logs.sh L124): duplicate of ai-board #2
- #4 (Copilot, capture-agent-logs.sh L125): duplicate of ai-board #2
