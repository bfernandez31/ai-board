# AI-BOARD Assist Result

## Status
SUCCESS

## Message
@Benoît Fernandez Fixed both security violations: CRED_VALUE_B64 is now masked in speckit.yml and ai-board-assist.yml, and error logging in credentials route is sanitized.

## Files Modified
- .github/workflows/speckit.yml
- .github/workflows/ai-board-assist.yml
- app/api/internal/credentials/route.ts

## Summary
Applied two security fixes:
1. Added `echo "::add-mask::${CRED_VALUE_B64}"` immediately after the base64 value is extracted in both speckit.yml and ai-board-assist.yml workflows to prevent log exposure
2. Replaced raw `error` logging with `error instanceof Error ? error.message : String(error)` in the credentials API catch block to prevent sensitive material (key fragments, IV values) from leaking in logs
