# AI-BOARD Assist Result

## Status
SUCCESS

## Message
@AI-BOARD Added scroll and resize event handlers to keep dropdown positioned correctly when the textarea scrolls.

## Files Modified
- components/comments/mention-input.tsx

## Summary
Fixed the dropdown positioning bug by adding scroll and resize event listeners:
- Extracted position calculation into reusable `updateDropdownPosition` callback
- Added useEffect that listens to scroll events (using capture phase for ancestors) and resize events
- Dropdown now follows the textarea when scrolling instead of remaining fixed in its initial position
- Event listeners are properly cleaned up when autocomplete closes
