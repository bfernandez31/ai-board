# AI-BOARD Assist Result

## Status
SUCCESS

## Message
@Benoît Fernandez Fixed passive module drawer bug — QUALITY_GATE and LAST_CLEAN cards no longer open the drawer or trigger invalid API requests.

## Files Modified
- components/health/health-dashboard.tsx
- components/health/health-module-card.tsx
- app/lib/hooks/useScanReport.ts

## Summary
Fixed regression where clicking passive module cards (QUALITY_GATE, LAST_CLEAN) opened a broken drawer with 400 API errors:
1. Guarded onClick in health-dashboard.tsx to only set selectedModule for active scan types
2. Added defense-in-depth guard in useScanReport to disable query for non-active types
3. Updated health-module-card to not show clickable styling (cursor-pointer, hover, role=button) when onClick is undefined
