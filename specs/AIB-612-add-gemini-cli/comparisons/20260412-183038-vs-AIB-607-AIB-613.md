# Code Implementation Comparison

## Executive Summary
- **Tickets Evaluated**: AIB-612 (source), AIB-607, AIB-613
- **Best Implementation**: AIB-612 with 89%
- **Key Differentiator**: Most comprehensive coverage — all 9 workflow files, full telemetry pipeline with Gemini cost estimation, analytics, and setup page integration
- **Recommendation**: Ship AIB-612

## Code Metrics (specs/ excluded)

| Ticket | Code Lines | Source Files | Test Files | Test Ratio |
|--------|------------|--------------|------------|------------|
| AIB-612 (source) | +843/-110 | 37 | 11 | 30% |
| AIB-607 | +773/-16 | 21 | 8 | 38% |
| AIB-613 | +820/-3 | 14 | 2 | 14% |

## Telemetry

| Ticket | Cost | Duration | Tokens (in/out) | Jobs | Model |
|--------|------|----------|-----------------|------|-------|
| AIB-612 (source) | $13.71 | 71m | 759K/151K | 4 | gpt-5.4 |
| AIB-607 | $22.27 | 59m | 45K/236K | 4 | claude-opus-4-6 |
| AIB-613 | N/A | 83m | N/A | 3 | N/A |

## Spec Fulfillment

| Requirement | AIB-612 | AIB-607 | AIB-613 | Best |
|-------------|---------|---------|---------|------|
| FR-001: GOOGLE credential provider | met | met | met | tie |
| FR-002: API_KEY + OAUTH_TOKEN | met | met | met | tie |
| FR-003: Validate Google credentials | met | met | partial | AIB-612/607 |
| FR-005: Gemini in agent selectors | met | met | partial | AIB-612/607 |
| FR-006: Gemini label + icon | met | met | met | tie |
| FR-007: Supported workflow execution | met | partial | not-met | AIB-612 |
| FR-008: Block unsupported workflows | met | partial | not-met | AIB-612 |
| FR-009: Credential at runtime | met | met | not-met | AIB-612/607 |
| FR-010: Job telemetry capture | met | met | not-met | AIB-612/607 |
| FR-011: Unavailable cost handling | met | met | not-met | AIB-612/607 |
| FR-012: Analytics dashboard Gemini | met | met | not-met | AIB-612/607 |
| FR-013: Analytics Mistral gap fix | met | met | not-met | AIB-612/607 |
| FR-014: Setup experience | met | met | not-met | AIB-612/607 |
| FR-015: Consistent agent definitions | met | met | not-met | AIB-607 |
| FR-016: No regression | met | partial | met | AIB-612 |

| Ticket | Spec Score |
|--------|-----------|
| AIB-612 | 97% |
| AIB-607 | 83% |
| AIB-613 | 27% |

## Implementation Choices Analysis

### [Spec] Supported workflow files updated for Gemini dispatch
| Ticket | Approach |
|--------|----------|
| AIB-612 | Updated ALL 9 workflow YAML files — speckit, quick-impl, iterate, verify, assist, health-scan, onboard, retro-spec. Explicit Gemini blocks in unsupported workflows with clear error messages. |
| AIB-607 | Updated 4 workflow files (iterate, quick-impl, speckit, and minor others). Missing verify, assist, health-scan, onboard, retro-spec updates. |
| AIB-613 | No workflow YAML changes at all. |

**Best**: AIB-612 — Complete coverage of all workflow dispatch paths with proper blocking.

### [Spec] Gemini telemetry and cost estimation
| Ticket | Approach |
|--------|----------|
| AIB-612 | Full telemetry route extension: parses `gemini_cli.api_response` and `gemini_cli.tool_call` events. Adds `estimateGeminiCost()` with pricing for gemini-2.5-pro/flash/2.0-flash. Adds `collect_gemini_telemetry()` in run-agent.sh with stream-json output parsing. |
| AIB-607 | Extended batch payload schema with `agent` and `costStatus` fields. Makes `costUsd` nullable (`number | null`) for unavailable-cost state. Skips Mistral cost estimation when agent is GEMINI. Does NOT add Gemini-specific OTLP parsing or cost estimation function. |
| AIB-613 | No telemetry changes. |

**Best**: AIB-612 — Complete telemetry pipeline including Gemini-specific cost pricing.

### Decision Point 1: Agent enum validation strategy
| Ticket | Approach | Constitution | Trade-offs |
|--------|----------|--------------|------------|
| AIB-612 | Mix of `z.nativeEnum(Agent)` (analytics, credential-check) and `z.enum([...])` (credentials route). Updated agent filter to use `z.union([z.literal('all'), z.nativeEnum(Agent)])` in analytics. | Mostly yes | Forward-compatible in critical paths |
| AIB-607 | Consistently uses `z.nativeEnum(Agent)` for analytics and credential-check. Uses `z.enum()` for setup jobs. Uses `Object.values(Agent)` for dynamic agent enumeration in analytics queries. | Yes | Best forward-compatibility; adding a new agent to Prisma enum auto-includes it |
| AIB-613 | Hardcoded `z.enum(['ANTHROPIC', 'OPENAI', 'MISTRAL', 'GOOGLE'])` in credentials route. | Partially | Requires manual update for new agents |

**Best**: AIB-607 — Most forward-compatible with `z.nativeEnum(Agent)` and `Object.values(Agent)` patterns.

### Decision Point 2: run-agent.sh Gemini CLI integration
| Ticket | Approach | Constitution | Trade-offs |
|--------|----------|--------------|------------|
| AIB-612 | Correct package `@google/gemini-cli`. OAuth via JSON file (`~/.gemini/oauth.json`). Stream-json output with post-exec telemetry collection. Env var: `GEMINI_OAUTH_JSON`. | Yes | Complete pipeline with telemetry scraping |
| AIB-607 | WRONG package `@anthropic-ai/gemini-cli` (does not exist). Uses `GEMINI_OAUTH_TOKEN` env var. Adds post-install binary check. Separate `setup_gemini_telemetry()` for native OTLP. | Partial — wrong package | Binary check is good but package is incorrect |
| AIB-613 | No run-agent.sh changes. | N/A | No workflow integration |

**Best**: AIB-612 — Correct package name and full telemetry collection.

### Decision Point 3: Google credential verification endpoint design
| Ticket | Approach | Constitution | Trade-offs |
|--------|----------|--------------|------------|
| AIB-612 | Extends existing `/api/credentials` route with GOOGLE provider. Splits API key (x-goog-api-key header) and OAuth (Bearer token + userinfo endpoint) verification paths. | Yes — extends existing patterns | Consistent with existing credential routes |
| AIB-607 | Extends existing `/api/credentials` route. Unified verification via models API for both credential types (API key via query param, OAuth via Bearer header). | Yes — cleaner unified path | Single endpoint, cleaner |
| AIB-613 | Creates NEW standalone routes `/api/credentials/google` and `/api/credentials/google/validate`. Duplicates logic from main credentials route. | No — creates parallel API surface | Violates existing patterns |

**Best**: AIB-607 — Unified models API endpoint for both types is cleaner. AIB-612 also good but uses separate userinfo endpoint for OAuth.

### Decision Point 4: Project root file creation
| Ticket | Approach | Constitution | Trade-offs |
|--------|----------|--------------|------------|
| AIB-612 | Adds `.npmignore` (utility file, acceptable) | Yes | Minor addition |
| AIB-607 | No root files | Yes | Clean |
| AIB-613 | Creates `GEMINI.md` (132 lines) at project root | **VIOLATION** — CLAUDE.md: "NEVER create README, GUIDE, or tutorial files at project root" | Direct violation of project rules |

**Best**: AIB-607 — No unnecessary root files. AIB-613 violates CLAUDE.md.

### Decision Point 5: Analytics agent filter extensibility
| Ticket | Approach | Constitution | Trade-offs |
|--------|----------|--------------|------------|
| AIB-612 | Changed `NamedAgent` type; uses `z.nativeEnum(Agent)` for analytics route; adds Mistral/Gemini labels in aggregations. | Yes | Good extensibility |
| AIB-607 | Changed `NamedAgent` to `Agent` type. Uses `Object.values(Agent)` for dynamic iteration. Uses `z.union([z.literal('all'), z.nativeEnum(Agent)])` for forward-compatible validation. | Yes — best pattern | Auto-extends when enum grows |
| AIB-613 | No analytics changes. | N/A | Missing |

**Best**: AIB-607 — Dynamic `Object.values(Agent)` iteration is the most future-proof.

## Constitution Compliance (Code Only)

| Ticket | I. TypeScript | II. Components | III. Tests | IV. Security | V. Database | Overall |
|--------|--------------|----------------|------------|--------------|-------------|---------|
| AIB-612 | Yes — proper types, z.nativeEnum | Yes — extends existing patterns | Yes — 11 test files | Yes — Zod validation, no exposed secrets | Yes — Prisma migration | 92% |
| AIB-607 | Yes — strong typing, Agent enum | Yes — extends existing patterns, adds agent-resolution util | Yes — 8 test files, dedicated suites | Yes — Zod validation | Yes — Prisma migration | 88% |
| AIB-613 | Partial — some loose types | No — creates parallel routes instead of extending | Partial — only 2 test files | Yes — Zod in new routes | Yes — Prisma migration | 50% |

**Notes:**
- AIB-607 loses points for wrong npm package name (would fail at runtime) and missing workflow files
- AIB-613 loses significant points for GEMINI.md at root (CLAUDE.md violation) and duplicate API routes

## Consolidated Specs Status
- [x] Feature warrants `specs/specifications/` update — new GOOGLE provider, GEMINI agent, analytics extensions
- [ ] Will be done in VERIFY stage

## Ranking & Recommendation

### 1. **AIB-612** - Best code (score: 89%)
**Why it wins**:
- Only implementation that updates ALL 9 workflow files for Gemini support
- Complete telemetry pipeline: Gemini event parsing + cost estimation pricing table
- Proper run-agent.sh with correct `@google/gemini-cli` package name
- Full setup page integration (both Mistral AND Gemini added)
- Comprehensive test coverage (11 test files, 30% ratio)
- Fixes Mistral gaps in analytics alongside Gemini addition

### 2. **AIB-607** - Strong code quality (score: 83%)
**Why it lost**:
- **Critical bug**: Wrong npm package `@anthropic-ai/gemini-cli` (should be `@google/gemini-cli`)
- Missing 5 workflow file updates (verify, assist, health-scan, onboard, retro-spec)
- No Gemini-specific cost estimation function
- Higher cost ($22.27 vs $13.71) for less complete coverage
- Better forward-compatible patterns (`z.nativeEnum`, `Object.values(Agent)`) but incomplete scope

### 3. **AIB-613** - Incomplete (score: 45%)
**Why it lost**:
- Creates `GEMINI.md` at project root (violates CLAUDE.md rules)
- Creates duplicate `/api/credentials/google` routes instead of extending existing patterns
- Creates standalone `lib/workflows.ts` with unused abstractions
- No workflow file updates, no telemetry, no analytics, no setup page
- Only covers credential storage — ~30% of spec requirements
- Only 2 test files

## Next Steps
- [ ] **Ship**: AIB-612
- [ ] **Close**: AIB-607, AIB-613
- [ ] **Update specs/specifications/**: Add GOOGLE provider, GEMINI agent documentation (VERIFY stage)
- [ ] **Consider**: Cherry-pick AIB-607's `z.nativeEnum(Agent)` and `Object.values(Agent)` patterns into AIB-612 for better forward-compatibility
