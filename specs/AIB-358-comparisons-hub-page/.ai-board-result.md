# AI-BOARD Assist Result

## Status
SUCCESS

## Message
@81817281 Fixed workflow dispatch failure being silently swallowed — the catch block now marks the job as FAILED and returns HTTP 502 instead of 201.

## Files Modified
- app/api/projects/[projectId]/comparisons/launch/route.ts

## Summary
Fixed the error handling in the comparison launch endpoint's workflow dispatch try/catch block:
- On dispatch failure: update the job record to FAILED status with completedAt timestamp
- Return HTTP 502 (Bad Gateway) with error message instead of silently returning 201
- The caller now correctly learns the workflow was not dispatched and the job won't stay stuck in PENDING
