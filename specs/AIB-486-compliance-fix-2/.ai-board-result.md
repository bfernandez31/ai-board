# AI-BOARD Assist Result

## Status
SUCCESS

## Message
@Benoît Fernandez Fixed both compliance issues directly in lib/validations/config.ts.

## Files Modified
- lib/validations/config.ts

## Summary
1. Added `.strict()` to `ServiceConfigSchema` — unknown fields in service entries are now rejected, matching all other section schemas.
2. Removed `collectUnknownFieldWarnings()` call from `validateConfig()` — replaced with empty warnings array since `.strict()` on all schemas now handles unknown field rejection via validation errors. Removed dead code: `collectUnknownFieldWarnings` function, `KNOWN_ROOT_KEYS`, and `KNOWN_SECTION_KEYS` constants.
