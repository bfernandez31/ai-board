# AI-BOARD Assist Result

## Status
SUCCESS

## Message
@Benoît Fernandez I moved the API-only `/api/tokens` unauthorized assertion out of the Playwright E2E suite and tightened the Vitest integration coverage instead.

## Files Modified
- tests/integration/auth/test-user-header-guard.test.ts
- tests/e2e/auth/test-user-header-redirect.spec.ts

## Summary
Applied a direct VERIFY-stage fix for the misplaced API-only test:
- Removed the Playwright request-context test that asserted `GET /api/tokens` returns `401`
- Strengthened the existing Vitest auth integration test to assert the full unauthorized JSON contract, including `code: AUTH_REQUIRED`
- Kept Playwright coverage focused on browser-required redirect and protected-page behavior
