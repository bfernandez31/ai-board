{
  "name": "ai-board-worker",
  "version": "1.0.0",
  "description": "AI Board workflow worker service",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --target node --outdir dist",
    "start": "bun run dist/index.js",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "bullmq": "^5.29.1",
    "ioredis": "^5.4.1",
    "@prisma/client": "^5.22.0",
    "zod": "^3.23.8",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "bun-types": "^1.1.38"
  }
}